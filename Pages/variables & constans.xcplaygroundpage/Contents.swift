var str = "hello playground"  //  var : 변수 (variable) - 언제든지 바뀔 수 있는 값 (mutable)

str = "hello world"

str

var number = 9
number = 10

var languge = "swift" // let : 상수 (constant) - 바뀌지 않는 값 (Immutable)
languge = "ObjectiveC"

// 규칙 1 - 띄어쓰기가 허용되지 않는다
let programminglanguge = "ObjectiveC"

// 규칙 2 - 카멜케이스 방법 사용
let favoriteLanguge = "Swift"

// 숫자로 시작할 수 없다
// 스페이스, 탭, 공백문자도 사용할 수 없다
// 수학적 기호도 사용할 수 없다

