//Brinary Operators ( + - / * %)
let height = 12.0
let width = 15.0

let area = height * width

let areaInSquareMeters = area / 10.764
// 10.764가 Double(소수점을 가진 숫자)이고 곱해줄 때 서로 같은 타입끼리 가능하기 때문에 height, width에 .0를 붙여주어 Double로 만들어주었다

// a = b 대입연산자
//Comparison Operator
let String1 = "Hello"
let String2 = "Hello"
let String3 = "hello"

String1 == String2
String1 == String3

String1 != String3
String1 != String2

2 > 1
"a" < "b"
"가" > "나"
"A" > "a"
2 < 1

2 <= 2

//Unary Operator (단항 연산자)
var level = 0
level = level + 1
level += 1 // level = level + 1 과 동일한 작업을 수행, 그러나 짧게 보인다

let on = true
let off = !on //느낌표는 반대








